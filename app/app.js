const nav=document.querySelector("#app-nav");const menu=document.querySelector(".menu-button");const views=[...document.querySelectorAll(".view")];const navItems=[...document.querySelectorAll(".nav-item")];function showView(id,push=true){const target=document.getElementById(id)||document.getElementById("overview");views.forEach(view=>view.classList.toggle("active",view===target));navItems.forEach(item=>{const active=item.dataset.view===target.id;item.classList.toggle("active",active);item.setAttribute("aria-current",active?"page":"false")});nav.classList.remove("open");menu.setAttribute("aria-expanded","false");if(push)history.replaceState(null,"","#"+target.id);target.scrollIntoView({behavior:matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth",block:"start"})}navItems.forEach(item=>item.addEventListener("click",()=>showView(item.dataset.view)));document.querySelectorAll("[data-open]").forEach(item=>item.addEventListener("click",()=>showView(item.dataset.open)));menu.addEventListener("click",()=>{const open=nav.classList.toggle("open");menu.setAttribute("aria-expanded",String(open))});document.addEventListener("click",event=>{if(!event.target.closest(".topbar")){nav.classList.remove("open");menu.setAttribute("aria-expanded","false")}});window.addEventListener("hashchange",()=>showView(location.hash.slice(1)||"overview",false));showView(location.hash.slice(1)||"overview",false);